import { IsLoggedDirective } from './is-logged.directive';

describe('IsLoggedDirective', () => {
  it('should create an instance', () => {
    const directive = new IsLoggedDirective();
    expect(directive).toBeTruthy();
  });
});
